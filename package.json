{
  "name": "vue3-test",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "eesbuild": "esbuild app.js --bundle --platform=node --outfile=out.js --loader: .js=.vue --minify --sourcemap"
  },
  "dependencies": {
    "vue": "^3.0.4"
  },
  "devDependencies": {
    "vite": "^1.0.0-rc.13",
    "@vue/compiler-sfc": "^3.0.4"
  }
}
